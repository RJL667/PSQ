# NOTE: the phishield-scanner service was RETIRED (2026-07-06). Production now
# runs on the Google Cloud VM (veilguard.phishield.com/scanner), tarball-deployed
# via security_scanner/deploy/deploy_vm.sh. Its Render service block was removed
# from this blueprint; suspend/delete the phishield-scanner service in the Render
# dashboard to complete decommission.
# NOTE: the sme-rating-engine service was RETIRED (2026-07-27). Production now
# runs on the Google Cloud VM (veilguard.phishield.com/smerating/), tarball-deployed
# via sme_rating_engine/deploy/deploy_sme_vm.sh. Its Render service block was removed
# from this blueprint; suspend/delete the sme-rating-engine service in the Render
# dashboard to complete decommission.
services:
  - type: web
    name: life-planner
    runtime: static
    staticPublishPath: .
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /planner.html
